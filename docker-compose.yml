services:
  web:
    build: .
    container_name: redream-astro
    restart: unless-stopped
    networks:
      - redream_net
      - caddy_net
    expose:
      - "4321"

networks:
  redream_net:
    name: redream_net
  caddy_net:
    external: true
    name: caddy_net
